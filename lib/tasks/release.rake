# coding: utf-8
# Legacy release task - disabled for Rails 7 compatibility
# require 'rake/packagetask'

PKG_VERSION = "6.1.0"
PKG_NAME = "typo"

task :release do
  puts "Release packaging is disabled in this upgraded version"
end
